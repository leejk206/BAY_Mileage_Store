---
phase: 02-program-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .env
autonomous: false
requirements: [CAT-01, CAT-02, PUR-01, PUR-02, PUR-03]

must_haves:
  truths:
    - "The upgraded Anchor program is live on devnet with the same program ID"
    - "StoreConfig PDA exists on-chain after initialize_store is called"
    - "A StoreItem PDA exists on-chain after add_item is called with a test item"
    - "A PurchaseReceipt PDA exists on-chain after purchase is called with sufficient balance"
    - "purchase fails on-chain when buyer balance is less than item price"
  artifacts:
    - path: ".env"
      provides: "STORE_CONFIG_PDA address for Phase 3 client scripts"
      contains: "STORE_CONFIG_PDA="
  key_links:
    - from: "deployer wallet (7otn8Pmbwys4gEAX5SwAFc9p695SJ9vDJyt2Biw9BDXA)"
      to: "program at 3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag"
      via: "anchor deploy using target/deploy/bay_mileage_store-keypair.json"
      pattern: "anchor deploy"
    - from: "initialize_store instruction"
      to: "StoreConfig PDA"
      via: "anchor client or solana CLI CPI call"
      pattern: "store_config.*seeds.*store_config"
---

<objective>
Deploy the upgraded program to devnet and run smoke-test calls to verify all three instructions work on-chain: initialize_store, add_item, and purchase (success + failure cases).

Purpose: This plan makes all 5 Phase 2 requirements live on devnet and confirms they work end-to-end before Phase 3 client scripts are built.

Output: Upgraded program on devnet + StoreConfig PDA initialized + test item registered + purchase receipt created + STORE_CONFIG_PDA address recorded in .env.
</objective>

<execution_context>
@C:/Users/Jk Lee/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jk Lee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-program-core/02-RESEARCH.md
@.planning/phases/02-program-core/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy upgraded program to devnet and run on-chain smoke tests via ts-mocha</name>
  <files>.env</files>
  <action>
    Step 1 — Deploy the upgraded program.

    From the workspace root (where Anchor.toml is), run:
    ```bash
    anchor deploy
    ```

    The deployer keypair is the default Solana CLI keypair (7otn8Pmbwys4gEAX5SwAFc9p695SJ9vDJyt2Biw9BDXA). Anchor 0.32 automatically uploads the IDL after deploy. If deploy fails with "insufficient funds", run `solana airdrop 2` first.

    Step 2 — Write a smoke-test script at `tests/smoke.ts`.

    Create `tests/smoke.ts` that exercises all three instructions using Anchor's `AnchorProvider` + `Program` client. Use the addresses from STATE.md:
    - Program ID: `3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag`
    - BAY Mint: `agYm2drYnvKnGQoXh1iZQ4iWYZPa87qtGe4baTNckdB`
    - Test Wallet: `GZuFe8aFbJNeg1HAj7fqzCjsRo18S9jaFTAY8cGLV8p3` (keypair at `wallets/test-wallet.json`)
    - Deployer: keypair at `~/.config/solana/id.json`

    The test script must:

    a) **initialize_store** — derive `store_config` PDA (`seeds = [Buffer.from("store_config")]`), call `program.methods.initializeStore().accounts({storeConfig, bayMint, authority, systemProgram}).signers([deployerKeypair]).rpc()`. Log the transaction signature.

    b) **add_item** — derive item PDA (`seeds = [Buffer.from("item"), Buffer.from("TestBadge")]`), call `program.methods.addItem("TestBadge", new BN(5_000_000), new BN(10)).accounts({item, storeConfig, authority, systemProgram}).signers([deployerKeypair]).rpc()`. Price is 5 BAY = 5_000_000 raw units. Log the signature.

    c) **purchase (success)** — derive `receipt` PDA (`seeds = [Buffer.from("receipt"), testWallet.publicKey.toBuffer(), itemPda.toBuffer()]`), find the test wallet's ATA for BAY mint, call `program.methods.purchase().accounts({buyer, buyerTokenAccount, bayMint, storeConfig, item, receipt, tokenProgram, associatedTokenProgram, systemProgram}).signers([testWalletKeypair]).rpc()`. Log the signature.

    d) **purchase (failure — PUR-03)** — create a second ATA-less scenario by using a fresh keypair with 0 BAY. Wrap the purchase call in try/catch and assert the error message contains "InsufficientFunds". Log "PUR-03: Insufficient funds correctly rejected".

    e) After all tests pass, derive `store_config` PDA address and append `STORE_CONFIG_PDA=<address>` to `.env`.

    Use `@project-serum/anchor` (or `@coral-xyz/anchor` — check `package.json` to confirm which package is installed) and `@solana/web3.js`.

    Run the test:
    ```bash
    anchor test --skip-local-validator
    ```

    If `anchor test` requires `[scripts]` configuration, alternatively run:
    ```bash
    npx ts-mocha -p tsconfig.json -t 60000 tests/smoke.ts
    ```

    Fix any TypeScript or RPC errors before proceeding.

    Step 3 — Record the StoreConfig PDA address in .env.

    After successful test run, the script will have appended `STORE_CONFIG_PDA=<address>` to `.env`. Confirm the line is present.
  </action>
  <verify>
    1. `anchor deploy` exit code 0 (or equivalent success message)
    2. `solana account 3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag --url devnet` shows the program is executable
    3. smoke test output shows 4 logged messages (3 tx signatures + PUR-03 rejection message)
    4. `.env` contains `STORE_CONFIG_PDA=` with a base58 pubkey value
    5. `solana account <STORE_CONFIG_PDA> --url devnet` returns account data (not "not found")
  </verify>
  <done>
    Program upgraded on devnet. initialize_store, add_item, and purchase all succeed via on-chain calls. A purchase with 0 BAY balance fails with InsufficientFunds. STORE_CONFIG_PDA is recorded in .env.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify on-chain state via Solana Explorer and balance check</name>
  <files>.env</files>
  <action>
    Pause for human verification of on-chain state. Automated smoke tests completed in Task 1. Human confirms results via Solana Explorer and CLI.
  </action>
  <verify>
    1. Check the StoreConfig PDA on Solana Explorer (devnet):
       - Get the PDA address from .env: `STORE_CONFIG_PDA` value
       - Visit: https://explorer.solana.com/address/&lt;STORE_CONFIG_PDA&gt;?cluster=devnet
       - Confirm the account is owned by program `3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag`

    2. Check the TestBadge item PDA:
       - Run: `node -e "const {PublicKey} = require('@solana/web3.js'); const [pda] = PublicKey.findProgramAddressSync([Buffer.from('item'), Buffer.from('TestBadge')], new PublicKey('3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag')); console.log(pda.toBase58());"`
       - Visit the resulting PDA on Solana Explorer (devnet) and confirm it has account data

    3. Confirm test wallet BAY balance decreased by 5 BAY:
       - Run: `spl-token balance agYm2drYnvKnGQoXh1iZQ4iWYZPa87qtGe4baTNckdB --owner GZuFe8aFbJNeg1HAj7fqzCjsRo18S9jaFTAY8cGLV8p3 --url devnet`
       - Expected: 99,995 BAY (was 100,000 BAY before purchase)

    4. Confirm smoke test terminal output showed "PUR-03: Insufficient funds correctly rejected"
  </verify>
  <done>All on-chain accounts verified, balance decreased correctly, PUR-03 rejection confirmed. Type "approved" to continue or describe issues.</done>
</task>

</tasks>

<verification>
1. Program at `3HgyGbc6oQvGMiWxMFCTfk4br1K4Zv6Jy2oR3chCcFag` is executable on devnet with upgraded code
2. StoreConfig PDA exists on-chain and is owned by the program
3. TestBadge StoreItem PDA exists on-chain with price=5_000_000 and stock=9 (after one purchase)
4. PurchaseReceipt PDA exists on-chain with correct buyer, item, and non-zero timestamp
5. Test wallet BAY balance decreased by exactly 5 BAY
6. `.env` contains STORE_CONFIG_PDA entry
7. Human verification checkpoint approved
</verification>

<success_criteria>
- `anchor deploy` succeeded
- All 5 Phase 2 requirements verified on-chain via smoke tests
- STORE_CONFIG_PDA recorded in .env for Phase 3
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-program-core/02-02-SUMMARY.md` following the summary template.
</output>
